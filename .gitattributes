# Shell scripts must stay LF so they run under bash (Git Bash / WSL / Linux).
# Without this, core.autocrlf=true rewrites them to CRLF and bash fails with
# "$'\r': command not found" (see specs/decisions.md 2026-06-24).
*.sh text eol=lf
