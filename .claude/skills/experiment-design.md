# Skill: experiment-design

- Start with smoke, then sanity, then paper-faithful runs.
- Change one scientific factor at a time.
- Keep backend and boost changes orthogonal.
- Save raw CSV/JSON before plotting.
- Record failure modes, not just successes.
